/* 01_break.wb

   Parse a single isolated break statement.  This might seem like an odd
   place to start, but parsing "break;" is very easy.  It's just two
   tokens (BREAK, SEMI).  So, write a rule that can parse that and
   return the corresponding BreakStatement() node in your model.
*/

break;

